import 'package:covid19/config/environment.dart';
import 'package:covid19/view/detail_page.dart';
import 'package:covid19/view/home_page.dart';
import 'package:flutter/material.dart';

void main() {
  runApp(
    MainApp(),
  );
}

class MainApp extends StatelessWidget {
  // TODO : buat untuk mengisi [routes] dan [initialRoute]
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      routes: null,
      initialRoute: null,
    );
  }
}
